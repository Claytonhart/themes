import React from "react";

const Navbar = () => {
  return (
    <header className="navbar bg-light">
      <div className="container">
        <h4 className="navbar__title">Start Bootstrap</h4>
        <button className="btn navbar__button">Sign In</button>
      </div>
    </header>
  );
};

export default Navbar;
